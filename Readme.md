# A Simple web interface to handle Success Matrices

To run please enter the following commands in the terminal after cloning the repo to run the application on localhost

    -   npm install
    -   npm run dev